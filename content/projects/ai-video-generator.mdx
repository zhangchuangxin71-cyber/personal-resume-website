---
title: "AI 短视频生成平台"
subtitle: "把文案、音频和字幕锁在同一条时间轴上"
role: "生成架构、工作区状态、字幕时间轴与前后端实现"
deliverable: "短视频成片 API / Web 向导"
period: "2026.06 - 2026.07"
status: "completed"
featured: false
order: 4
summary: "以文案为主时间轴，统一分句、配音、字幕和素材绑定，解决多阶段生成时的错位、漏字和样式漂移。"
problem: "画面、音频和字幕分别生成时容易产生时序漂移；LLM 分句还可能漏字、改写原文，最后表现为字幕错位、破词、闪屏和语义割裂。"
constraints:
  - "原文不能被模型遗漏或改写"
  - "画面、配音和字幕必须共享稳定时间基准"
  - "用户需要在自动生成后手动修订并锁定分句"
  - "横竖屏、多分辨率下字幕都要保持可读"
approach: "把双时序方案重构为文案驱动的单一生成链路。用原文内联标签约束 LLM 分句，音频改为按句上传 URL 并在后端规范化拼接，字幕以 SRT 锁定时间轴、ASS 负责样式；服务端拆成分句、配音、预览、合成四阶段任务，前端用工作区向导完成审核和配置。"
flow:
  - "输入文案"
  - "语义分句"
  - "素材匹配"
  - "配音生成"
  - "字幕渲染"
  - "成片导出"
decisions:
  - title: "让文案成为主时间轴"
    context: "画面与字幕使用不同切分和时序来源时，任何一侧的变化都会造成音画字幕错位。"
    choice: "先锁定文案片段，再让素材、音频和字幕全部挂载到同一片段。"
    reasoning: "后续编辑只改变明确的片段边界，三种媒体不会独立漂移。"
  - title: "以内联标签约束分句"
    context: "让模型自由返回改写后的句子，会出现漏字、篡改和顺序变化。"
    choice: "模型只在原文中插入结构标签，结果再由确定性规则清洗。"
    reasoning: "保留原始字符序列，同时仍能利用模型理解语义边界。"
  - title: "把长流程拆成四阶段任务"
    context: "分句、配音、预览和合成耗时、失败原因和重试成本不同。"
    choice: "FastAPI 分别建立 Task，统一调度、状态和错误码。"
    reasoning: "失败可以在局部阶段重试，前端也能给出清楚进度并允许中途编辑。"
metrics:
  - value: "6 类"
    label: "核心质量问题已解决"
    verified: true
  - value: "4×"
    label: "提示词生成并发提升"
    verified: true
  - value: "2 种"
    label: "横竖屏输出规格"
    verified: true
technologies:
  - "LLM"
  - "Minimax T2A"
  - "ASS"
  - "FastAPI"
  - "jieba"
cover: "/media/ai-video-generator-cover.webp"
coverAlt: "一条文案纸带依次通过分句、波形、字幕和视频成片阶段的概念视觉"
---

## 实现细节

这个项目的核心不是“生成一个 mp4”，而是把整条流水线做成可恢复、可预览、可审计的 workspace。

### 关键阶段

1. `POST /api/v1/split/run`：LLM 语义分句，可顺带生成检索词与提示词
2. `POST /api/v1/audio/process/run`：逐句拉取音频 URL，生成 `master.wav`、`subtitle.srt` 和 `subtitle.ass`
3. `POST /api/v1/media/bind/run`：按句绑定视频或图片素材，并把时长写回计划
4. `POST /api/v1/visual/preview/run`：只改字幕样式，不重算 cue 起止
5. `POST /api/v1/compose/run`：最终合成并上传 OSS，成功后删除 workspace

### 工作区里保存了什么

- `config/split_plan.json`：分句锁定结果
- `config/audio_config.json`：音频 revision 和字幕时间轴
- `subtitles/subtitle.srt`、`subtitles/subtitle.ass`：时间轴与样式层分离
- `intermediate/compose/{job_id}/`：合成临时文件，任务结束即清理
- `deliverables/output.mp4`：最终长期交付文件

### 为什么这么设计

这套代码最关键的决策是把“时间轴”与“样式”分开，把“中间结果”与“最终交付”分开。这样用户在预览阶段可以反复改字体、偏移和素材，而不会破坏已经确认好的句子边界和字幕节奏。

## 最难的问题

### 模型理解不能以破坏原文为代价

自由生成式分句会引入文本幻觉。改为内联标签后，模型只负责判断边界，字符完整性由程序验证，彻底解决漏字和篡改问题。

### 预览必须允许人接管

五步向导允许用户编辑并锁定分句、选择平台规格、实时查看封面和调整字幕样式。自动化负责减少机械工作，关键表达仍由用户确认。

### 工作区必须在成功后收口

如果 workspace 永久保留，后续改稿会把历史中间产物和新结果混在一起。当前实现选择在 compose 成功后删除 workspace，只保留 OSS 成片和归档记录，这样每次生成都是一次独立、干净的发布动作。

## 交付与影响

- 完成从文案输入、人工复核到多规格视频导出的产品闭环
- 沉淀异步任务、工作区、字幕和成片的端到端接口闭环

## 反思与下一步

当前结果已经稳定解决文本与时序一致性。后续可把素材匹配质量单独量化，并补充失败素材的替换建议和局部重新生成能力。
